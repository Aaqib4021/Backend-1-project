<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen p-10 bg-zinc-900 text-white">
      <h3 class="text-3xl mb-4 tracking-tighter">Login Account</h3>
      <p class="text-red-700 font-semibold mb-2"><%= errorMessage%></p>
      <form action="/login" method="post">
        <input
          type="email"
          placeholder="email"
          name="email"
          class="bg-transparent outline-none px-4 py-2 border-2 border-zinc-700 rounded-lg"
        />
        <input
          type="password"
          placeholder="password"
          name="password"
          class="bg-transparent outline-none px-4 py-2 border-2 border-zinc-700 rounded-lg"
        />
        <input
          type="submit"
          value="Login Account"
          class="bg-blue-500 rounded-lg px-4 py-2"
        />
      </form>
    </div>
  </body>
</html>
