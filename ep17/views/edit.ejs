<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen p-10 bg-zinc-900 text-white">
      <div class="w-full flex justify-end">
        <a class="bg-red-500 rounded-md px-3 py-2" href="/logout">Logout</a>
      </div>
      <h5 class="mb-3">Edit your post</h5>
      <form action="/update/<%=post._id%>" method="post">
        <textarea
          placeholder="whats on your mind?"
          name="content"
          class="resize-none bg-transparent w-1/3 outline-none border border-gray-700 rounded-lg px-4 pt-4 block"
        >
<%=post.content%></textarea
        >
        <input
          type="submit"
          value="Update post"
          class="px-4 py-2 bg-blue-500 block mt-2 rounded-lg w-40 text-black font-semibold"
        />
      </form>
    </div>
  </body>
</html>
